# CLAUDE.md

## CRITICAL RULES

- **NEVER checkout any branch other than `main` without explicit user request.** All feature branches must live in git worktrees. Use `git worktree add` instead of `git checkout`.
